# ── LaTeX build artifacts ─────────────────────────────────────────────────────
report/*.aux
report/*.bbl
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.out
report/*.synctex.gz
report/*.toc
report/*.lof
report/*.lot
report/*.nav
report/*.snm
report/*.vrb
report/*.bcf
report/*.run.xml

# Keep the compiled PDF and source
!report/*.pdf
!report/*.tex

# IEEEtran.cls was downloaded locally — don't commit it (it's a third-party file)
report/IEEEtran.cls

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
*.whl

# Virtual environments
.venv/
venv/
env/
.env

# Jupyter / IPython
.ipynb_checkpoints/
*.ipynb

# ── OS files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*~
*.swp
*.swo

# ── VS Code ───────────────────────────────────────────────────────────────────
# Keep workspace settings (recipes, etc.) but ignore user-specific state
.vscode
.vscode/*.code-workspace

# ── Benchmark results (raw CSVs) ──────────────────────────────────────────────
# Uncomment the line below if you do NOT want to commit raw results
# results/*.csv

# ── Misc ──────────────────────────────────────────────────────────────────────
*.tmp
*.bak

# ── Native build artifacts ────────────────────────────────────────────────────
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# Local benchmark binaries
bin/*
!bin/.gitkeep

# ── lmbench generated files ───────────────────────────────────────────────────
lmbench/bin/
lmbench/Results/
lmbench/results/
lmbench/src/*.o
lmbench/src/*.a
lmbench/src/*.s
lmbench/src/git.ver

# ── Generated experiment outputs ──────────────────────────────────────────────
results/*.csv
results/*.txt
results/*.log

# Generated figures from analysis scripts
report/figures/*.pdfShort answer: Partially done.

✅ Main pipeline (custom C benchmarks + plots + report PDF) is done.
❌ Full “real” pipeline is not fully done because lmbench Phase-1 build/run failed in logs.
⚠️ Current lmbench/NUMA/eBPF files exist, but at least some are fallback/projected data (not fully fresh real measurements).

# Legacy generated report file at repo root
SSP_Paper.pdf
